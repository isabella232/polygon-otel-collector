name: polygonreceiver

attributes:
  chain:
    description: The name of a chain.

metrics:
  polygon.last_block:
    enabled: true
    description: The current block number.
    unit: block
    sum:
      value_type: int
      aggregation: cumulative
      monotonic: false
    attributes: [chain]

  polygon.submit_checkpoint:
    enabled: true
    description: Latest checkpoint transaction.
    unit: 1
    sum:
      value_type: int
      aggregation: cumulative
      monotonic: false
    attributes: [chain]
  
  polygon.last_block_time:
    enabled: true
    description: The average block time.
    unit: milliseconds
    gauge:
      value_type: int
    attributes: [chain]

